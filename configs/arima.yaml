store_type: "c"

model:
  name: arima
  order: [5, 1, 6]
  trend: t       # d=1 → 'c' bị loại bỏ bởi differencing, dùng 't' thay thế (tương đương constant trên chuỗi sai phân)
  skip_scaling: true    # ARIMA dùng giá trị gốc, không cần StandardScaler

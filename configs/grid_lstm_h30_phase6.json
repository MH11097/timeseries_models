{
  "_comment": "Phase F: h64-L2 + dropout sweep. Rationale: Phase C showed dropout has zero effect at L1 (no inter-layer path). L2 activates inter-layer dropout. Fixed: multioutput, seq_len=21, wd=0.0, lr=0.0005 (same as baseline h64-L1). Grid: 4 combos.",
  "forecast_strategy": "multioutput",
  "model.seq_len": [21],
  "model.weight_decay": [0.0],
  "model.learning_rate": [0.0005],
  "model.hidden_size": [64],
  "model.num_layers": [2],
  "model.dropout": [0.1, 0.2, 0.3, 0.4]
}

model:
  name: xgboost
  skip_scaling: true
  n_estimators: 500
  max_depth: 9
  learning_rate: 0.05
  subsample: 1
  colsample_bytree: 0.8
  reg_alpha: 1.5
  reg_lambda: 2.5
  early_stopping_rounds: 50
